# merkle-tree

A merkle tree implementation in Rust. Supports arbitrary hash sizes, inclusion proof generation/validation and (most likely) second preimage attack mitigation.  

This project is still in progress.
